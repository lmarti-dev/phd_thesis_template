#import "../utils/template.typ": *
#import "../bib_state.typ": bib_state
#context bib_state.get()

#heading("Appendix",numbering: (..)=>[],outlined: true)
#show: appendix
  
  You can also write stuff after you're done with the main text if that's your thing.

  == And the numbering will change
  === As expected of the Appendix
  === And so on
  ==== And so forth
  == It should follow the rules
  === As expected

